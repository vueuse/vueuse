{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "outputs": [
        "*.d.mts",
        "*.js",
        "*.mjs"
      ]
    },
    "//#docs:build:vitepress": {
      "outputs": [
        "packages/.vitepress/dist/**"
      ]
    }
  }
}
