---
category: Utilities
---

# multiply

`MULTIPLY` operation for refs.

## Usage

```ts
import { multiply } from '@vueuse/core'

const a = ref(5)
const b = ref(10)

const product = multiply(a, b)
```

## Related Functions

- `add`
- `subtract`
- `divide`
