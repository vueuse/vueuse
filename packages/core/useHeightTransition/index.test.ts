import { useHeightTransition } from '.'

describe('useHeightTransition', () => {
  it('should be defined', () => {
    expect(useHeightTransition).toBeDefined()
  })

  it('should be update counter', () => {
    // const { count, inc, dec, get, set, reset } = useHeightTransition()

    expect(0).toBe(0)
  })
})
