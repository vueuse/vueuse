{
  "extends": ["//"],
  "tasks": {
    "update": {
      "dependsOn": ["@vueuse/metadata#build"],
      "outputs": [
        "skills/**",
        "$TURBO_ROOT$/skills/vueuse-functions/**"
      ]
    }
  }
}
